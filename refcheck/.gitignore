# Made by scripts/refcheck (see README.md): the full fixture corpus and baseline runs are regenerated, never
# committed. Only fixtures-mini/ and the three corpus states copied into fixtures-late/ are.
corpus/
baseline/
