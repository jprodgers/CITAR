# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.12.0.schema.json
#
# winget installer manifest for CITAR.
#
# Submitted to microsoft/winget-pkgs, not published from this repository. See ../README.md.
# InstallerSha256 must be the hash of the exact file at InstallerUrl; the validation pipeline
# downloads it and checks.

PackageIdentifier: JimmieRodgers.CITAR
PackageVersion: 0.1.2
InstallerType: inno
# Per-user, matching how the installer is built: no elevation prompt, and it works for somebody
# without administrator rights on their own machine.
Scope: user
InstallModes:
  - interactive
  - silent
  - silentWithProgress
UpgradeBehavior: install
ReleaseDate: 2026-09-21
InstallerSwitches:
  # Inno's own switches. /NORESTART matters: without it a silent install may reboot the machine.
  Silent: /VERYSILENT /SUPPRESSMSGBOXES /NORESTART
  SilentWithProgress: /SILENT /SUPPRESSMSGBOXES /NORESTART
  Log: /LOG="<LOGPATH>"
  InstallLocation: /DIR="<INSTALLPATH>"
ExpectedReturnCodes:
  - InstallerReturnCode: 2
    ReturnResponse: cancelledByUser
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/jprodgers/CITAR/releases/download/v0.1.2/CITAR-0.1.2-setup.exe
    InstallerSha256: 5E3832EC12A51D9A6D1E853F4160F7FDA69A9C1499B94DEB63EC57F198940CF1
ManifestType: installer
ManifestVersion: 1.12.0
